
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const InteractionTracker = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Interaction Tracker</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Coming soon: Track your interactions, set check-in reminders, and maintain meaningful connections.
        </p>
      </CardContent>
    </Card>
  );
};

export default InteractionTracker;
